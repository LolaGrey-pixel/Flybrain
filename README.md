# FlyBrain + FisHKA v4

Prototype of a coupled-oscillator classification framework
with PCM-based features (Phase Correlation Matrix).

## Requirements

A local HTTP server is required — Web Workers do not work over `file://`.

## Run locally

```bash
python -m http.server 8000